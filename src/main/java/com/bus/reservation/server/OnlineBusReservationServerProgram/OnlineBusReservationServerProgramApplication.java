package com.bus.reservation.server.OnlineBusReservationServerProgram;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class OnlineBusReservationServerProgramApplication {

	public static void main(String[] args) {
		SpringApplication.run(OnlineBusReservationServerProgramApplication.class, args);
	}
}
